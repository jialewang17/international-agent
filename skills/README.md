# Skills

当前仅启用国际传播技能：

| 文件 | skill_id | 说明 |
|------|----------|------|
| `intl-comm.md` | `intl_comm` | 关键词匹配后注入流程说明，引导调用 `intl_comm_reply` |

配置见 `config/skills.yaml`（`discover_files: false`，不自动发现其它 md）。
